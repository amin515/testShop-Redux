
// create shop reducers

const shopReducers = (state = 0, {type, payload}) => {
  switch (type) {
    case "INIT":
        return state
    
  
    default:
    return state
  }
}

// export 
export default shopReducers;